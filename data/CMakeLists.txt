if (UNIX)
  set (DESTDIR "${T4KCOMMON_DATA_PREFIX}")
elseif(WIN32)
  set (DESTDIR "$ENV{APPDATA}/${PACKAGE_NAME}")
endif (UNIX)

install (DIRECTORY images
    DESTINATION ${DESTDIR}
    PATTERN Makefile* EXCLUDE
    PATTERN .svn EXCLUDE
    PATTERN CMakeLists.txt EXCLUDE
    PATTERN *~ EXCLUDE )
